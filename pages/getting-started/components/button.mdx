### Import

```tsx filename="Exemple.tsx"
import { Button } from "@xefi/x-react/Button";
```

Button Component exporte 2 composants :

- `Button` : Le composant principal pour afficher un bouton.
- `ButtonGroup` : Un composant wrapper pour afficher un groupe de boutons.

<h3 className="font-bold text-xl mt-10"># Colors</h3>

<Tabs items={["Preview", "Code"]}>
 <Tabs.Tab>
      <div className="border border-border rounded-lg px-4 py-6">
   {["primary", "secondary", "success", "danger", "warning"].map((color) => (
     <Button key={color} color={color} className="mr-6" rounded="sm">
       {color.charAt(0).toUpperCase() + color.slice(1)}
     </Button>
   ))}
   </div>
 </Tabs.Tab>
 <Tabs.Tab>

```tsx filename="Exemple.tsx"
import { Button } from "x-react/button";

export default function Example() {
  const colors = ["primary", "secondary", "success", "danger", "warning"];

  return (
    <>
      {colors.map((color) => (
        <Button key={color} color={color} className="mr-6" rounded="sm">
          {color.charAt(0).toUpperCase() + color.slice(1)}
        </Button>
      ))}
    </>
  );
}
```

 </Tabs.Tab>
</Tabs>

<h3 className="font-bold text-xl mt-10"># Variants</h3>

<Tabs items={["Preview", "Code"]} className="mt-6">
 <Tabs.Tab>
     <div className="border border-border rounded-lg px-4 py-6">
   {["solid", "bordered", "faded", "shadow"].map((variant) => (
     <Button key={variant} variant={variant} color="primary" className="mr-6" rounded="sm">
       {variant.charAt(0).toUpperCase() + variant.slice(1)}
     </Button>
   ))}
   </div>
 </Tabs.Tab>
 <Tabs.Tab>

```tsx filename="Exemple.tsx"
import { Button } from "x-react/button";

export default function Example() {
  const variants = ["solid", "bordered", "faded", "shadow"];

  return (
    <>
      {variants.map((variant) => (
        <Button
          key={variant}
          variant={variant}
          color="primary"
          className="mr-6"
          rounded="sm"
        >
          {variant.charAt(0).toUpperCase() + variant.slice(1)}
        </Button>
      ))}
    </>
  );
}
```

 </Tabs.Tab>
</Tabs>

<h3 className="font-bold text-xl mt-10"># Avec icons</h3>

<Tabs items={["Preview", "Code"]}>
 <Tabs.Tab>
     <div className="border border-border rounded-lg px-4 py-6">
   {[<IconPlus />, <IconTrash />].map((icon, i) => (
     <Button key={i} color="primary" className="mr-6" rounded="sm" startContent={icon}>
      Avec icon
     </Button>
   ))}
     </div>
 </Tabs.Tab>
 <Tabs.Tab>

```tsx filename="Exemple.tsx"
import { Button } from "x-react/button";

export default function Example() {
  const variants = ["solid", "bordered", "faded", "shadow"];

  return (
    <>
      {variants.map((variant) => (
        <Button
          key={variant}
          variant={variant}
          color="primary"
          className="mr-6"
          rounded="sm"
        >
          {variant.charAt(0).toUpperCase() + variant.slice(1)}
        </Button>
      ))}
    </>
  );
}
```

 </Tabs.Tab>
</Tabs>

### Props

<Table />

import { Table } from "@/components/Table";
import { Button } from "x-react/button";
import { IconPlus, IconTrash } from "x-react/icons";

import { Tabs } from "nextra/components";
